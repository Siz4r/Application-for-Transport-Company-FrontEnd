export interface Address {
  city: string;
  street: string;
  postalCode: string;
  buildingNumber: number;
}

export interface RegisterData {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
}
